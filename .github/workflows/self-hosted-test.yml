# Copyright(c) The Maintainers of Nanvix.
# Licensed under the MIT License.

name: "Self-Hosted Test"

on:
  workflow_dispatch:

  pull_request:
    types:
      - "opened"
      - "synchronize"
      - "reopened"
      - "ready_for_review"
    branches:
      - "main"

  push:
    branches:
      - "main"

# Cancel previous running actions for the same PR
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  CARGO_INCREMENTAL: 0
  PYTHONUTF8: "1"
  RUSTC_WRAPPER: sccache
  SCCACHE_GHA_ENABLED: "true"

jobs:
  # ==========================================================================================
  # Release: Prepare Distribution Draft (Merged PRs)
  # ==========================================================================================

  prepare-release-distributions:
    name: "Prepare Release Distribution Draft"
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    outputs:
      release-id: ${{ steps.release.outputs.release-id }}

    steps:
      - name: "Checkout CI Scripts"
        uses: actions/checkout@v6
        with:
          sparse-checkout: scripts/ci

      - name: "Prepare GitHub Release"
        id: release
        if: |
          github.event_name == 'push' &&
          github.ref == 'refs/heads/main' &&
          github.repository == 'nanvix/distro'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
        run: scripts/ci/self-hosted-test.sh prepare-release

  # ==========================================================================================
  # Linux: Build, Unit Test & Smoke Test (Self-Hosted)
  # ==========================================================================================

  linux-build-test:
    name: "Linux Build & Test"
    needs: prepare-release-distributions
    timeout-minutes: 120
    runs-on:
      group: Test
      labels: [ self-hosted, Linux, docker ]
    permissions:
      contents: write
      actions: write

    steps:
      - name: "Checkout Pre-Checkout Cleanup Script"
        uses: actions/checkout@v6
        with:
          path: .ci-bootstrap
          sparse-checkout: scripts/ci/self-hosted-test.sh
          sparse-checkout-cone-mode: false

      - name: "Pre-Checkout Cleanup"
        shell: bash
        run: .ci-bootstrap/scripts/ci/self-hosted-test.sh pre-checkout-cleanup

      - uses: actions/checkout@v6
        with:
          # OpenSSL carries optional upstream test submodules that are not part
          # of the distro build. Only materialize this repository's gitlinks.
          submodules: true

      - name: "Clean Submodule Build Artifacts"
        shell: bash
        run: scripts/ci/self-hosted-test.sh clean-submodule-build-artifacts

      - name: "Relocate Cargo State"
        shell: bash
        run: scripts/ci/self-hosted-test.sh relocate-cargo-state

      - name: "Setup KVM"
        shell: bash
        run: scripts/ci/self-hosted-test.sh setup-kvm

      - name: "Setup sccache"
        uses: Mozilla-Actions/sccache-action@v0.0.10
        with:
          version: "v0.10.0"

      - name: "Install Python Dependencies"
        shell: bash
        run: scripts/ci/self-hosted-test.sh install-python-dependencies

      - name: "Check Distro Tooling"
        shell: bash
        run: scripts/ci/self-hosted-test.sh check-distro-tooling

      - name: "Ensure Docker Daemon"
        shell: bash
        run: scripts/ci/self-hosted-test.sh ensure-docker-daemon

      - name: "Build"
        shell: bash
        run: scripts/ci/self-hosted-test.sh build

      - name: "Create Distribution Images"
        shell: bash
        run: scripts/ci/self-hosted-test.sh create-distribution-images

      - name: "Smoke Test BusyBox Distribution (KVM)"
        shell: bash
        run: scripts/ci/self-hosted-test.sh smoke-test-busybox

      - name: "Smoke Test Composed Distribution"
        shell: bash
        run: scripts/ci/self-hosted-test.sh smoke-test-composed

      - name: "Package Release Distribution Images"
        if: |
          github.event_name == 'push' &&
          github.ref == 'refs/heads/main' &&
          github.repository == 'nanvix/distro'
        shell: bash
        run: scripts/ci/self-hosted-test.sh package-release-distributions

      - name: "Reclaim Build Space"
        shell: bash
        run: scripts/ci/self-hosted-test.sh reclaim-build-space

      - name: "Test"
        shell: bash
        env:
          RUST_LOG: debug
        run: scripts/ci/self-hosted-test.sh test

      - name: "Stage Release Distribution Images"
        if: |
          github.event_name == 'push' &&
          github.ref == 'refs/heads/main' &&
          github.repository == 'nanvix/distro'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
          RELEASE_ID: ${{ needs.prepare-release-distributions.outputs.release-id }}
        run: scripts/ci/self-hosted-test.sh stage-release-distributions

      - name: "Remove Relocated Cargo State"
        if: always()
        shell: bash
        run: scripts/ci/self-hosted-test.sh remove-relocated-cargo-state

      - name: "Print sccache Statistics"
        if: always()
        shell: bash
        run: scripts/ci/self-hosted-test.sh print-sccache-statistics

      - name: "Upload logs in case of failures"
        if: failure() || cancelled()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: linux-self-hosted-logs-${{ github.event.pull_request.number ||
            github.run_number }}
          overwrite: "true"
          path: |
            nanvix/logs/
            nanvix/**/*.log
            nanvix/**/*.out
            nanvix/**/*.err
            build/dist/busybox/busybox-smoke.log
            build/dist/ci-composed/composed-smoke.log
            ci-logs/

  # ==========================================================================================
  # Publish: Distribution Images (Merged PRs)
  # ==========================================================================================

  publish-release-distributions:
    name: "Publish Release Distribution Images"
    needs: [ prepare-release-distributions, linux-build-test, windows-build-test ]
    if: |
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      github.repository == 'nanvix/distro'
    runs-on: ubuntu-24.04
    permissions:
      contents: write

    steps:
      - name: "Checkout CI Scripts"
        uses: actions/checkout@v6
        with:
          sparse-checkout: scripts/ci

      - name: "Publish GitHub Release"
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
          RELEASE_ID: ${{ needs.prepare-release-distributions.outputs.release-id }}
        run: scripts/ci/self-hosted-test.sh publish-release

  # ==========================================================================================
  # Windows: Build & Test (Self-Hosted)
  # ==========================================================================================

  windows-build-test:
    name: "Windows Build & Test (${{ matrix.build-type }})"
    needs: prepare-release-distributions
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        build-type:
          - ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' &&
            github.repository == 'nanvix/distro' && 'release' || 'debug' }}
        machine-type: [ microvm ]
    runs-on:
      group: Test
      labels: [ self-hosted, Windows ]
    permissions:
      contents: write
      actions: write

    steps:
      - name: "Checkout Pre-Checkout Cleanup Script"
        uses: actions/checkout@v6
        with:
          path: .ci-bootstrap
          sparse-checkout: scripts/ci/self-hosted-test.ps1
          sparse-checkout-cone-mode: false

      - name: "Clean Workspace (remove leftover junctions)"
        shell: pwsh
        run: .\.ci-bootstrap\scripts\ci\self-hosted-test.ps1 clean-workspace

      - uses: actions/checkout@v6
        with:
          submodules: true

      - name: "Clean Submodule Build Artifacts"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 clean-submodule-build-artifacts

      - name: "Create Short Drive Mapping"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 create-short-drive-mapping

      - name: "Restore Directory Symlinks as Junctions"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 restore-directory-symlinks

      - name: "Refresh PATH from Registry"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 refresh-path

      - name: "Set Rust Environment"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 set-rust-environment

      - name: "Isolate Cargo Home"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 isolate-cargo-home

      - name: "Verify Rust Toolchain"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 verify-rust-toolchain

      - name: "Setup Prerequisites"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 setup-prerequisites

      - name: "Setup sccache"
        uses: Mozilla-Actions/sccache-action@v0.0.10
        with:
          version: "v0.10.0"

      - name: "Install Python Dependencies"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 install-python-dependencies

      - name: "Check Distro Tooling"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 check-distro-tooling

      - name: "Build & Test Nanvix Core"
        shell: pwsh
        env:
          MACHINE_TYPE: ${{ matrix.machine-type }}
          LOG_LEVEL: ${{ matrix.build-type == 'release' && 'error' || 'trace' }}
          TEST_LOG_LEVEL: ${{ matrix.build-type == 'release' && 'debug' || 'trace' }}
          RELEASE_FLAG: ${{ matrix.build-type == 'release' && 'RELEASE=yes' || '' }}
        run: .\scripts\ci\self-hosted-test.ps1 build-test-nanvix-core

      - name: "Download Pinned Distribution Guests"
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: .\scripts\ci\self-hosted-test.ps1 download-pinned-distribution-guests

      - name: "Create Distribution Images"
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 create-distribution-images

      - name: "Smoke Test BusyBox Distribution (WHP)"
        shell: pwsh
        env:
          RUST_LOG: debug
        run: .\scripts\ci\self-hosted-test.ps1 smoke-test-busybox

      - name: "Package Release Distribution Images"
        if: |
          matrix.build-type == 'release' &&
          github.event_name == 'push' &&
          github.ref == 'refs/heads/main' &&
          github.repository == 'nanvix/distro'
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 package-release-distributions

      - name: "Stage Release Distribution Images"
        if: |
          matrix.build-type == 'release' &&
          github.event_name == 'push' &&
          github.ref == 'refs/heads/main' &&
          github.repository == 'nanvix/distro'
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
          RELEASE_ID: ${{ needs.prepare-release-distributions.outputs.release-id }}
        run: .\scripts\ci\self-hosted-test.ps1 stage-release-distributions

      - name: "Print sccache Statistics"
        if: always()
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 print-sccache-statistics

      - name: "Remove Drive Mapping"
        if: always()
        shell: pwsh
        run: .\scripts\ci\self-hosted-test.ps1 remove-drive-mapping

      - name: "Upload logs in case of failures"
        if: failure() || cancelled()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: windows-self-hosted-logs-${{ github.event.pull_request.number ||
            github.run_number }}-${{ matrix.build-type }}-${{
            matrix.machine-type }}
          overwrite: "true"
          path: |
            nanvix/logs/
            nanvix/**/*.log
            nanvix/**/*.out
            nanvix/**/*.err
            build/dist/busybox/busybox-smoke.log
